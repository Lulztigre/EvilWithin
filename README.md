# EvilWithin
Implementing a backend/frontend mechanism to dynamically intercept and hook a JavaScript function at runtime in order to extract symmetric encryption keys used by the application for securing request and response payloads.
